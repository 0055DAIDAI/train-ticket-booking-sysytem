<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.StopMapper">

<select id="findByRouteId" resultType="org.example.pojo.Stop">
    SELECT id, route_id, station_id, departure_time, arrival_time, sequence
    FROM stop
    WHERE route_id = #{routeId}
    ORDER BY sequence
</select>

</mapper>
